import logging from 'dummy/utils/logging';
import { module, test } from 'qunit';

module('Unit | Utility | logging');

// Replace this with your real tests.
test('it works', function(assert) {
  let result = logging();
  assert.ok(result);
});
